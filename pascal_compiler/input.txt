var n,i,k,a:integer;
begin
writeln('Enter n');
readln(n);
a:=0;
for i:=1 to n do begin
                 writeln('Enter ',i:1,'number');
                 readln(a);
                 if a mod 2=0 then k:=k+1;
                 end;
writeln('Number of even',k);
readln;
end.